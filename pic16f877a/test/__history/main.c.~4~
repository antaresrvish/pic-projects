#include <main.h>

void main()
{
   setup_comparator(NC_NC_NC_NC);
   setup_adc(ADC_OFF);

   while(TRUE)
   {
      output_low(PIN_A0); //19?
      delay_ms(1000);
      output_high(PIN_A0);
      delay_ms(500);
   }

}
